apiVersion: v1
kind: Secret
metadata:
  name: postgresql-secret
  namespace: wil-uat
type: Opaque
stringData:
  POSTGRES_USER: "wil"
  POSTGRES_PASSWORD: "changeme"
  POSTGRES_DB: "wil"

# Replace values above with real credentials before applying.
# Consider using SealedSecrets or External Secrets for production.
