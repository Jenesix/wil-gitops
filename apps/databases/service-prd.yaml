apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: wil-prd
spec:
  selector:
    app: postgresql
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None  # Headless service สำหรับ StatefulSet
