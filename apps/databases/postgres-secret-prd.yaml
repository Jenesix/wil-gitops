apiVersion: v1
kind: Secret
metadata:
  name: postgresql-secret
  namespace: wil-prd
type: Opaque
stringData:
  POSTGRES_USER: "wil"
  POSTGRES_PASSWORD: "changeme-production"
  POSTGRES_DB: "wil"

# Replace values above with real credentials before applying.
# Use SealedSecrets or an external secret manager for production.
