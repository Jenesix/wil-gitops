apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: wil-prd
type: Opaque
stringData:
  REDIS_PASSWORD: "CHANGE_ME_PROD_REDIS_PASSWORD"

# IMPORTANT: Replace with a strong password and consider using SealedSecrets
# or ExternalSecrets to avoid storing plain secrets in git.
